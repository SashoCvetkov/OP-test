#include<stdio.h>
#include<math.h>
 
int main()
{
    float radius, high, S, V;
    printf("radius: ");
    scanf("%f", &r);
    printf("height: ");
    scanf("%f", &h);
    
      if (r <= 0 || h <= 0)
    {
        printf("Negative numbers.\n");
        return 1;
    }
    
    S = M_PI * radius *radius;
    V = M_PI * radius * radius * height;
    printf("S = %.2f\n", S);
    printf("V = %.2f\n", V);
    return 0;  
    
   
}
