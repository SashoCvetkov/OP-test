#include<stdio.h>
#include<math.h>
 
int main()
{
    float r, h, S, V;
    printf("radius: ");
    scanf("%f", &r);
    printf("height: ");
    scanf("%f", &h);
    
    
    S = M_PI * r *r;
    V = M_PI * r * r * h;
    printf("S = %.2f\n", S);
    printf("V = %.2f\n", V);
    return 0;  
    
     if (r <= 0 || h <= 0)
    {
        printf("Negative numbers.\n");
        return 1;
    }
}
